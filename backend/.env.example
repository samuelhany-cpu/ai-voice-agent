# Port for this gateway server
PORT=3000

# URL of the n8n webhook that handles the audio
# For MVP, this might be a local tunnel or cloud URL
N8N_WEBHOOK_URL=http://localhost:5678/webhook/voice-input

# Toggle Mock Mode to test without n8n running (true/false)
MOCK_MODE=true
